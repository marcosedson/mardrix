export default function ProdutosPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Produtos</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Estrutura do módulo: listagem + formulário + BFF em /api/produtos.
      </p>
    </div>
  );
}

